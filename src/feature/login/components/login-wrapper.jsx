import { stylex } from '@stylexjs/stylex';
import React from 'react';

const styles = stylex.create({
  root: {},
});

export const LoginWrapper = () => {
  return <div id="globalContainer"></div>;
};
