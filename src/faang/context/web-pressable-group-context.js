import { createContext } from 'react';

const initial = undefined;

/**
 * @type {import("react").Context<import("./types").WebPressableGroupContextProps | undefined>}
 */
export const WebPressableGroupContext = createContext(initial);
